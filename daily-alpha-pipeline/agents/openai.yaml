interface:
  display_name: "Daily Alpha Pipeline"
  short_description: "Orchestrate macro, event, and hedging signals into a daily long-short book."
  default_prompt: "Use $daily-alpha-pipeline to run the full hedge fund workflow and return the final portfolio report as strict JSON."
