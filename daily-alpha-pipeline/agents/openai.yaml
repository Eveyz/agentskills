interface:
  display_name: "Daily Alpha Pipeline"
  short_description: "Run the four upper-layer engines end to end into a daily portfolio output."
  default_prompt: "Use $daily-alpha-pipeline to run the complete end-to-end workflow through top-down, idea, risk, and execution layers and return a readable Markdown report plus the matching structured JSON payload."